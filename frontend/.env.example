# Base URL of the Spring Boot backend.
# Defaults to http://localhost:8080 when unset, which is correct for local dev.
# Copy this file to .env and edit only if the backend runs elsewhere.
VITE_API_BASE_URL=http://localhost:8080
