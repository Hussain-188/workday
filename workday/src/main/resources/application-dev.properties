# Development profile. Activate with:
#   .\mvnw.cmd spring-boot:run "-Dspring-boot.run.profiles=dev"
#
# Seed data is inserted by DevDataSeeder (Java, profile-guarded) rather than a
# migration, so passwords are hashed with the real PasswordEncoder instead of
# being committed as literal BCrypt strings.
logging.level.com.hackathon.workday=DEBUG
